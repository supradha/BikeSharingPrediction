Running the code:

Create a python environment using conda and the JDA_Analysistask_env.yaml file using the following command from the source folder:
conda env create –f JDA_Analysistask_env.yaml

To activate the environment:
conda activate JDA_Analysistask_env

All analysis steps are provided in jupyter notebook. Run it with the following command from the source folder: 
jupyter notebook 
The results of analysis and prediction model execution is in JDA_BikesharingAnalysistask.ipynb. 